<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/main_chart_container"
    android:padding="@dimen/small_padding">

    <TextView
        android:id="@+id/txtv_chart_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/basic_text"
        android:text="Title"
        android:gravity="center_horizontal"
        android:layout_marginTop="@dimen/small_padding"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/txtv_chart_title_extra"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/basic_text"
        android:gravity="center_horizontal"
        android:text="Info"
        android:textSize="@dimen/textAppearanceBody1"
        app:layout_constraintStart_toStartOf="@+id/txtv_chart_title"
        app:layout_constraintTop_toBottomOf="@+id/txtv_chart_title"
        app:layout_constraintEnd_toEndOf="@+id/txtv_chart_title" />

</androidx.constraintlayout.widget.ConstraintLayout>